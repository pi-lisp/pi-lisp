Good — proceeding to `eval.rs` with the design as specified (single interval arg for now; I'll flag the generalization point inline rather than block on it).

Let me look at the current `eval.rs` in full first, since I need to match its existing reduction strategy exactly (call-by-value? normal order? how `THComp`/`TTransport` etc. already reduce) before bolting ι-reduction onto it.Now I understand the strategy clearly: this is **eager, normal-order evaluation to a normal form** — `eval` always recurses into subterms before deciding (eval children first, then pattern-match on the evaluated head to decide whether to reduce further), congruence on everything that doesn't have a special rule, "stuck" forms are just the syntactic reconstruction with evaluated children.

Now let's design the three new cases for this file.

## What needs to reduce

**1. `TCon` / `TPCon` — congruence only.** These are data, not redexes by themselves: `eval` just evaluates their children and rebuilds.

**2. `TElim(motive, cases, scrut)` — this is where ι-reduction lives:**
- Evaluate `scrut`.
- If it's `TCon(_, c, args)`: find the case for `c`, β-substitute `args` for the case's binders into `body`, eval the result. (Standard ι-reduction.)
- If it's `TPCon(_, c, args, r)`: evaluate `r`.
  - If `r` evaluates to `I0`/`I1` (via the same `is_top_dnf`/`is_bot_dnf`/literal check pattern `reduce_papp_by_type` uses in `equality.rs`, but here I need it standalone since `eval.rs` doesn't depend on `equality.rs`): substitute `args` for the case's ordinary binders, then evaluate the case body's `PApp` at that same `I0`/`I1` boundary — i.e. exactly the "tube reduces to its boundary" pattern already used for `THComp`.
  - If `r` is a free/neutral interval term (a DNF that's neither top nor bottom, e.g. a variable): substitute `args` into the binders, leaving the case body as a `PLam` over the interval var, then `PApp` it to `r`. This stays a genuine `PApp` redex point — eval recurses and it'll either reduce further or get stuck as neutral, matching how `PApp`/`PLam` already behave elsewhere in this evaluator.
  - If `scrut` is neither `TCon` nor `TPCon` (neutral, e.g. `TVar`/stuck application): the whole `TElim` is stuck — rebuild with evaluated motive/cases/scrutinee.

This mirrors the THComp pattern (`is_top_dnf` → reduce to tube's `I1` face; `is_bot_dnf` → reduce to base; else stuck) almost exactly, which is reassuring — it means I'm not inventing a new reduction shape, just reusing the existing one for path constructors.

One wrinkle: interval values in this codebase are represented as `Term::TCube(DNF)` once evaluated (see `Term::TInterval(i) => Term::TCube(eval_interval(i))`), and top/bottom are checked via `is_top_dnf`/`is_bot_dnf` against that DNF — *not* by pattern-matching `Term::TInterval(I::I0)` directly post-eval. I need to follow that exact convention, not the raw-`I0`/`I1` check `equality.rs`'s `reduce_papp_by_type` uses as a *fallback* (that function checks both, since pre-eval terms can still be syntactically `TInterval(I::I0)`). I'll check both forms defensively, same as `reduce_papp_by_type` does, since `r` here could come from a not-yet-`eval`'d caller too in principle — but since I will call `eval(r)` myself first, `is_top_dnf`/`is_bot_dnf` should suffice; I'll add the literal fallback anyway for robustness/cheapness, matching the existing defensive style.

Let me write it.I want to double-check the `subst_case_args` fold direction carefully, since this is exactly the kind of off-by-one that compiles but silently substitutes the wrong argument into the wrong slot. Let me reason through it concretely with a 2-binder example, then verify against `beta`'s definition rather than trust my own restatement.

`beta(body, arg) = shift(-1, 0, subst(0, shift(1, 0, arg), body))` — substitutes index 0, then closes the gap.

For two binders `[x, y]` (outermost-first, so in the body, `y` is index 0 and `x` is index 1) with args `[ax, ay]` (same order — `ax` for `x`, `ay` for `y`):
- First `beta(body, ay)` substitutes index 0 (`y`) with `ay`, closes → now what was index 1 (`x`) is index 0.
- Then `beta(result, ax)` substitutes the new index 0 (`x`) with `ax`, closes.

So the fold needs to process `ay` first, then `ax` — i.e. **reverse order of `args`**, processing innermost-bound (last in the outermost-first list) first. `args.iter().rev()` gives `[ay, ax]`, folding left-to-right means `ay` applied first, then `ax` applied to that result. That matches. Good — the code is correct, but let me verify this isn't just me re-deriving the same possibly-wrong intuition twice. Let me test it concretely.Confirmed empirically, not just by re-deriving the same reasoning a second time. `subst_case_args` is correct as written: `binders`/`args` outermost-first, fold processes innermost (last) first.

Now let's compile the whole crate and see what's left to fix:`eval.rs` compiles clean — only `equality.rs` (`term_size`) and `typechecker.rs` remain, which are next on the plan. Let me sanity-check the new `eval.rs` logic with a tiny throwaway end-to-end test for `Bool`/`Nat` before moving to the typechecker, so I catch any logic bug now rather than after three more files depend on it.This is the `infer` function — exactly where I'll need to add real typechecking logic for `TData`/`TCon`/`TPCon`/`TElim` next anyway, not just a stub. For this isolated smoke test of `eval.rs` only, I'll add a temporary `todo!()`-panic wildcard so the crate compiles; my test will only exercise `eval`, not `infer`, so it's safe.Compiles. Now write an actual test binary exercising `eval` on `Bool` and `Nat` (with recursion through `TCon`) to verify ι-reduction works correctly, including the "stuck on neutral scrutinee" case.Builds clean. Now run it:Good — this caught a real bug, but in my **test**, not in `eval.rs`. Two important signals here:

1. `not(true)`/`not(false)` ι-reduction **works correctly** — both `TElim`-on-`TCon` cases reduced as expected.
2. The "1+3" test failed because I miscounted in my own test scaffolding (`got` = 5 i.e. `1+4`, not `1+3`) — `plus_term(k.clone(), n.clone())` was called with `k=1` (the original `nat_suc(nat_zero)`, "1") *not decremented*, since I never actually decremented `k` recursively before passing it in. Let me check: actual result is `suc^5(zero) = 5`, expected was `suc^4(zero) = 4`. The bug is in my hand-rolled `plus_term`/test wiring (off-by-one in how I constructed the expected case body), not in `eval`'s `TElim` logic itself, which faithfully executed exactly the term I gave it.

Let me fix the test to verify the actual ι-reduction mechanics more directly and rigorously — by reducing `TElim` one real step at a time and checking each step, rather than pre-baking recursion by hand (which is error-prone, as I just demonstrated to myself).Now let me also add a real two-argument-binder test (since `Nat`'s `suc` only has one binder — I should test a constructor with ≥2 ordinary args to actually exercise the telescope-substitution fold direction I verified separately above, this time through the real `eval.rs` code path):All four checks pass, including the multi-binder telescope test that specifically targets the fold-direction bug I was worried about. The `eval.rs` logic is now verified, not just argued for:

- ι-reduction on `TCon` correctly dispatches to the matching case and substitutes.
- Multi-argument constructors substitute each argument into its correct binder slot (verified `b`, not `a`, was selected — this is the test that would have caught a reversed fold).
- Neutral scrutinees correctly leave `TElim` stuck, rebuilt with evaluated children, matching every other "stuck" form in this evaluator.

I haven't yet exercised the `TPCon`/path-constructor branch (boundary reduction at `I0`/`I1`, and the neutral-interval-var case) since that needs `eval_interval`/`is_top_dnf`/`is_bot_dnf` wiring that's worth its own focused test — I'll fold that into this same testing pass before calling `eval.rs` done, rather than deferring it to "later."Let me view that block precisely to confirm what's actually being tested:I see a real bug here that my smoke test didn't catch because it never exercised `TPCon`: `body_at_r = subst_case_args(ord_binders, args, &case.body)` substitutes the ordinary args using `subst_case_args`'s telescope logic, which assumes the binders being substituted are **innermost-most-recent in `body`** (i.e. index 0 = last binder in the list passed in). But per the `ElimCase` doc comment I wrote in `syntax.rs`, the case's `body` is `PLam`-shaped — meaning the **interval variable**, not the last ordinary arg, is the innermost binder (index 0) in `body`. The ordinary args sit at indices `1..arity` (shifted up by one), not `0..arity-1`.

So calling `subst_case_args(ord_binders, args, &case.body)` directly is wrong: it would substitute `args` for indices `0..arity-1`, clobbering the interval variable's slot instead of skipping past it. Let me verify this concretely before fixing, since it's exactly the kind of subtlety that's easy to get backwards twice in a row.Confirmed: the bug is real. `b` (which should resolve to `Var(200)`) instead resolved to `Var(100)` (`a`'s value) — the substitution is shifted by one position because it didn't account for the interval binder sitting at index 0. The fix: shift the ordinary-arg substitution targets by 1 (to skip past the interval variable's slot) before applying them, or equivalently substitute starting at index 1 instead of index 0.

Let me write a correct helper and fix `eval.rs`:Fixed and verified. Note this approach is simpler than I expected: since we're substituting *into* a `PLam` body **without removing the `PLam` binder** (the interval var stays bound — we want a `PApp` redex, not a fully-closed term), there's no shifting/closing needed at all, just `subst` at offset indices, no `beta`. This is actually cleaner than reusing `subst_case_args`. Let me also double check the substituted `args` themselves don't need shifting — since they're closed terms from the *caller's* scope being placed under one extra `PLam` binder relative to where they originally lived... 

Wait — I need to be careful here. Let's check: do `args` need to be shifted by 1 before insertion, since they're going under the `PLam`'s binder? Let's check what `subst(j, s, term)` does when `term` contains a nested binder — looking back at `syntax.rs`'s `subst` for `PLam`: it does `shift(1, 0, s)` before recursing. That's `subst`'s *own* internal handling of crossing one binder. But here I'm calling `subst(idx, arg, &result)` directly on the **already-PLam-bound body**, so `subst`'s `PLam` case will fire if `result` is literally a `PLam` node — but `result` (the case body) might already be a `PLam` whose *internal* body I want to substitute into, or `result` could itself be that inner body if I've already unwrapped one layer. Let me check the actual call site contract precisely.Good catch on my own test — that's `subst`'s standard behavior: when crossing a binder it shifts the substituted value by 1 (since the value moves into a scope with one more bound variable), so the indices *inside the result* are offset. That's actually **correct and expected** (it's exactly how `Var(100)`/`Var(200)` represent free variables from an outer scope, which legitimately need shifting to remain valid once placed under a new binder — that's not a bug, it's `subst` doing its job correctly per its own contract). The "off by 1" here is an artifact of my test using bare `Var(100)`/`Var(200)` as opaque free-variable markers; in the real code, `args` are full closed terms (e.g. `Term::TVar(0)` referring to something in the *caller's* surrounding context, or ground terms like `TCon("Nat","zero",[])` with no free variables at all), so this shifting is precisely what keeps those free references valid as they move under the new `PLam` binder. This matches `shift(1,0,s)` being applied internally by `subst`'s own `PLam`/`TAbs`/etc. cases everywhere else in `syntax.rs` — it's the file's standing convention, not a special case I'm introducing.

So: the correct fix is to call `subst` at indices `0..arity-1` directly on the `PLam`-shaped case body (relying on `subst`'s own existing per-binder shift logic to handle crossing the `PLam`), **not** my intermediate "offset by 1 manually" version, and **not** the original `subst_case_args`/`beta`-based version (which incorrectly closes/removes the binder it substitutes, which is wrong here since we want to substitute into an *open* term without consuming a binder).Now let's verify with a real test through the actual `eval.rs` code path (not my standalone mock), exercising both the boundary case (`r` = `I0`/`I1`) and the neutral-interval case.I overwrote my temporary scratch stubs when copying the real files back in. Let me reapply just the temp stubs needed for this isolated test (same as before):Now add `TPCon`/HIT tests (an `S¹`-style case: `base`, `loop`, with boundary reduction and a stuck/neutral interval test):This caught a real second bug. The `Term::TElim` evaluation on `TPCon` correctly reduced `loop @ 0`'s case body to a `PApp` of `PLam(i. loop @ i)` applied to `I0`, but `PApp`'s own eval rule — `Term::PLam(_, body) => eval(&beta(&body, &r_))` — requires `r_` (the already-evaluated interval arg) to literally match `Term::PLam` on the left, which it does here; the problem is `beta`'s substitution puts `r_` (which is `Term::TCube(DNF{cubes:{}})`, i.e. evaluated-bottom, not literally `Term::TInterval(I::I0)`) into the body, producing `loop @ (TCube bottom)` — which is itself a *new* `TPCon` whose interval arg is a `TCube`, not further reduced because my `TElim` handler isn't re-entered (we already exited it; eval just re-evaluates the `PApp`, which evaluates the `TPCon` via congruence, not by going back through `TElim`).

In other words: the bug isn't in substitution — it's that **reducing `elim(loop @ r)`'s case unfolds to `(PLam i. loop @ i) @ r`, which beta-reduces to `loop @ r` again** (since my test's case body is literally the identity-shaped `loop @ i`), so the elim never actually "uses" the boundary information — it just regenerates the same neutral `TPCon`. This is a flaw in my **test's case body**, not in the underlying `TElim`/`TPCon` eval mechanics: I picked a degenerate case body (`PLam i. loop(i)`) that doesn't actually consume the boundary, so of course evaluating it just gives back `loop(r)`. A real eliminator case for `loop` targeting `motive = \_. S1` would have to produce a path between `case_base` and `case_base` (since `S1`'s `loop` boundaries are both `base`) — e.g. `PLam i. base`, not `PLam i. loop(i)`. Let me fix the test:But wait — this also weakens the test: a constant path `PLam i. base` would reduce to `base` at *any* `r`, including a neutral variable, via `PApp`'s eval rule re-evaluating `beta(body, r_)` where `body` doesn't mention the bound variable at all. That means my "neutral interval var should stay stuck" assertion would now incorrectly fail (it would also reduce to `base`, not stay neutral) — but that's actually *correct behavior*, not a bug: a genuinely constant path **should** reduce to its value regardless of `r`, even when `r` is a free variable, since `PApp(PLam(_, body), r)` always beta-reduces once we have a literal `PLam` on the left, independent of whether `r` itself is a normal form. Let me reconsider what I actually want to test for "neutral stays stuck," since a constant-path case doesn't probe that at all.

The genuinely interesting "stuck" case is when the **scrutinee itself** (not the case body) has a neutral interval argument — i.e. `loop(i)` where `i` is a free variable, being eliminated. In that situation, `scrut_ = eval(loop(i)) = TPCon(...)`, which **does** match the `TPCon` arm of my `TElim` match (it's a syntactic constructor regardless of whether its interval argument happens to be neutral), so it will *still* reduce via the case — and that's actually correct cubical behavior: a HIT eliminator is allowed to compute on `loop(i)` for **any** `i`, free or not, producing a path application that may or may not further reduce depending on the case body. There's no "stuck" state for `TPCon` scrutinees the way there is for ordinary neutral terms like `TVar`. My original "neutral" test was conceptually testing the wrong thing. Let me fix the test to check the actually-meaningful neutral case: a scrutinee that is a bare neutral term (`TVar`), not a `TPCon` with a neutral interval index.All tests pass now, including the boundary reduction (`I0`, `I1`), the free-interval-variable case (correctly computing rather than incorrectly staying stuck), and the genuinely-neutral-scrutinee case (correctly staying stuck). This caught and fixed a real substitution bug (the off-by-one in `TPCon`'s ordinary-argument substitution relative to the still-open interval binder) that the earlier `TCon`-only tests couldn't have caught.

Let me copy the fixed `eval.rs` back to the main scratch dir and do one final full-crate compile check before showing you the result.Confirmed clean: only `equality.rs` and `typechecker.rs` (next steps, untouched) remain. `eval.rs` itself is fully correct and verified.

## Summary of this step

**File:** `eval.rs`

**Added:**
- `TCon`/`TPCon` — congruence cases (eval children, rebuild).
- `TElim` — ι-reduction:
  - on `TCon` scrutinees: dispatch to the matching case, substitute args via `subst_case_args` (closes the case body — verified correct fold direction empirically, twice, including via a 2-binder test through the real code path).
  - on `TPCon` scrutinees: substitute ordinary args via the new `subst_ord_args_open` (does **not** close the trailing interval binder), then `PApp` at the constructor's interval argument — letting the existing `PApp`/`PLam` eval rule (and `THComp`-style boundary collapse) do the rest.
  - on anything else: stuck, rebuilt with evaluated children, matching every other stuck form in this file.

**Bugs caught and fixed by testing rather than by inspection:**
1. First version conflated "substitute and close" (`subst_case_args`/`beta`) with "substitute but leave the trailing `PLam` binder open" — these need different helpers, and using the wrong one for `TPCon` silently shifted every ordinary argument's binding by one slot.
2. My own first test for "neutral interval var" was testing a non-bug (a `TPCon` scrutinee with a free interval index is valid HIT behavior, not a stuck state) — fixed by adding the actually-meaningful stuck case (a bare `TVar` scrutinee) instead.

Both required actually running code, not just re-reading the diff — exactly the kind of thing that looks plausible on inspection and is wrong.

Ready for `typechecker.rs` and `equality.rs` next — let me know which you'd like first, or if you want them together since they're the two remaining exhaustiveness errors.